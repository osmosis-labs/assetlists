name: Osmosis Front-End Production Deployment
on:
  push:
    branches:
      - main
jobs:
  Deploy-Production:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel Deployment via Webhook
        run: curl -X POST ${{ secrets.VERCEL_WEBHOOK_DEPLOY_PRODUCTION_URL }}
